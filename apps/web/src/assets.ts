export const SHELL = `<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="color-scheme" content="light">
    <title>Factory</title>
    <link rel="stylesheet" href="/assets/app.css">
  </head>
  <body>
    <a class="skip-link" href="#content">Skip to evidence</a>
    <div id="app" aria-live="polite"><p class="loading">Loading Factory evidence…</p></div>
    <script src="/assets/app.js" defer></script>
  </body>
</html>`

export const STYLES = `
:root{font-family:Inter,ui-sans-serif,system-ui,-apple-system,sans-serif;color:#18201c;background:#f3f1ea;font-synthesis:none;--ink:#18201c;--muted:#68726c;--line:#d8d8ce;--paper:#fbfaf6;--accent:#d9653b;--accent-dark:#8d3d22;--forest:#214e3c;--warning:#8a4b13;--warning-bg:#fff1d7;--danger:#8f2e2a;--danger-bg:#fbe7e3;--good:#256345;--good-bg:#e3f2e9;--blue:#315b7a;--blue-bg:#e6eff5;--shadow:0 18px 50px rgba(34,43,37,.07)}
*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;min-width:0;background:radial-gradient(circle at 90% 0,#e1ebe2 0,transparent 30rem),var(--paper);line-height:1.45}button,a{font:inherit}.skip-link{position:fixed;left:-999px;top:1rem}.skip-link:focus{left:1rem;background:#fff;padding:.7rem 1rem;border:2px solid var(--ink);z-index:10}:focus-visible{outline:3px solid var(--accent);outline-offset:3px}
.shell{max-width:96rem;margin:auto;padding:2rem 2.5rem 5rem}.masthead{display:flex;justify-content:space-between;align-items:flex-start;gap:2rem;padding:1rem 0 2rem;border-bottom:1px solid var(--line)}.brand{display:flex;align-items:center;gap:.8rem}.mark{display:grid;place-items:center;width:2.2rem;height:2.2rem;border-radius:.65rem;background:var(--ink);color:#fff;font-weight:800}.eyebrow{margin:0;color:var(--accent-dark);font-size:.72rem;font-weight:800;letter-spacing:.13em;text-transform:uppercase}.masthead h1{margin:.08rem 0;font-size:clamp(1.65rem,3vw,2.4rem);line-height:1}.subtitle{margin:.45rem 0 0;color:var(--muted);max-width:42rem}.head-actions{display:flex;align-items:center;gap:.75rem;flex-wrap:wrap;justify-content:flex-end}.branch{padding:.45rem .7rem;border-radius:999px;background:#e7ebe4;color:var(--forest);font-size:.82rem;font-weight:700}.button{border:1px solid var(--ink);background:var(--ink);color:#fff;border-radius:.55rem;padding:.55rem .8rem;font-weight:700;cursor:pointer}.button:hover{background:var(--forest)}.button.secondary{background:transparent;color:var(--ink);border-color:var(--line)}.button.secondary:hover{border-color:var(--ink);background:#fff}.button.small{padding:.4rem .62rem;font-size:.78rem}.button:disabled{cursor:not-allowed;opacity:.5}
.nav{display:flex;gap:.4rem;overflow:auto;padding:1rem 0}.nav a{color:var(--muted);text-decoration:none;padding:.5rem .7rem;border-radius:.5rem;white-space:nowrap;font-size:.88rem;font-weight:700}.nav a:hover{color:var(--ink);background:#eeece5}.summary{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:.7rem;margin:1rem 0 2.2rem}.metric{min-width:0;padding:1rem 1.05rem;border:1px solid var(--line);border-radius:.8rem;background:rgba(255,255,255,.58)}.metric strong{display:block;font-size:1.55rem;line-height:1.1}.metric span{display:block;margin-top:.35rem;color:#56615b;font-size:.8rem}.metric.attention{border-color:#e6b37c;background:var(--warning-bg)}
.layout{display:grid;grid-template-columns:minmax(0,1.45fr) minmax(19rem,.75fr);gap:1.2rem;align-items:start}.column{display:grid;gap:1.2rem}.panel{min-width:0;border:1px solid var(--line);border-radius:1rem;background:rgba(255,255,255,.76);box-shadow:var(--shadow);overflow:hidden}.panel-head{display:flex;align-items:flex-end;justify-content:space-between;gap:1rem;padding:1.2rem 1.25rem;border-bottom:1px solid var(--line)}.panel-head h2{margin:0;font-size:1.05rem}.panel-head p{margin:0;color:#56615b;font-size:.8rem}.list{display:grid}.item{min-width:0;padding:1.05rem 1.25rem;border-bottom:1px solid var(--line)}.item:last-child{border-bottom:0}.item-head{display:flex;justify-content:space-between;gap:1rem;align-items:flex-start}.item h3{margin:0;font-size:.95rem}.meta{margin:.25rem 0 0;color:#59645e;font-size:.8rem}.mono{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;overflow-wrap:anywhere}.badges{display:flex;flex-wrap:wrap;gap:.35rem;justify-content:flex-end}.badge{display:inline-flex;align-items:center;gap:.3rem;border-radius:999px;padding:.27rem .52rem;background:#ecece5;color:#46514b;font-size:.72rem;font-weight:800;letter-spacing:.025em;text-transform:uppercase}.badge.good{color:var(--good);background:var(--good-bg)}.badge.warn{color:var(--warning);background:var(--warning-bg)}.badge.danger{color:var(--danger);background:var(--danger-bg)}.badge.info{color:var(--blue);background:var(--blue-bg)}.button.good{background:var(--good);border-color:var(--good)}.button.danger{background:var(--danger);border-color:var(--danger)}.button.danger.secondary{background:transparent;color:var(--danger);border-color:#d39a92}.button.danger.secondary:hover{background:var(--danger-bg)}.external{display:inline-block;margin-top:.7rem;color:var(--blue);font-weight:700;text-underline-offset:.18rem}.body-copy{margin:.75rem 0 0;color:#354139;font-size:.88rem}.sublist{display:grid;gap:.5rem;margin-top:.8rem}.subitem{padding:.7rem .75rem;border-left:3px solid var(--line);background:#f7f6f1;border-radius:0 .55rem .55rem 0}.subitem.high{border-color:var(--accent);background:#fff6eb}.actions{display:flex;flex-wrap:wrap;gap:.45rem;margin-top:.85rem}.action-note{margin:.75rem 0 0;color:#59645e;font-size:.8rem}.limitations{margin:.65rem 0 0;padding-left:1.1rem;color:var(--warning);font-size:.82rem}.empty{padding:1rem 1.25rem;color:var(--muted);text-align:center}.response{margin:.75rem 0 0;max-height:10rem;overflow:auto;padding:.75rem;background:#252c28;color:#e9eee9;border-radius:.55rem;font:.78rem/1.5 ui-monospace,SFMono-Regular,Menlo,monospace;white-space:pre-wrap}.status{position:fixed;right:1.25rem;bottom:1.25rem;max-width:min(28rem,calc(100vw - 2.5rem));padding:.8rem 1rem;border-radius:.65rem;background:var(--ink);color:#fff;box-shadow:var(--shadow);font-size:.85rem;z-index:4}.status.error{background:var(--danger)}.error-page{max-width:42rem;margin:12vh auto;padding:2rem;border:1px solid #e2b4aa;border-radius:1rem;background:#fff}.error-page h1{margin:.3rem 0}.loading{padding:4rem;color:var(--muted);text-align:center}
@media(max-width:1050px){.summary{grid-template-columns:repeat(3,1fr)}.layout{grid-template-columns:1fr}}
@media(max-width:620px){.shell{padding:1rem 1rem 4rem}.masthead{display:grid;gap:1rem}.head-actions{justify-content:flex-start}.summary{grid-template-columns:repeat(3,1fr);gap:.5rem}.metric{padding:.75rem}.metric strong{font-size:1.25rem}.nav{margin:.35rem 0;display:grid;grid-template-columns:repeat(6,1fr);gap:.25rem;overflow:visible;padding:.65rem 0}.nav a{grid-column:span 2;padding:.4rem .2rem;text-align:center;background:#f1efe8}.nav a:nth-child(n+4){grid-column:span 3}.panel-head{padding:1rem;align-items:flex-start;flex-direction:column;gap:.25rem}.item{padding:1rem}.item-head{display:grid;gap:.55rem}.badges{justify-content:flex-start}.response{max-height:8rem}}
@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}
#decisions{margin-bottom:1.2rem}.choice-group{padding:1.25rem}.choice-group+.choice-group{border-top:1px solid var(--line)}.choice-group-head{margin-bottom:1rem}.choice-group-head h3{margin:0;font-size:1.1rem}.choice-group-head p{margin:.3rem 0 0;color:#59645e;font-size:.85rem}.choice-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1rem;align-items:start}.choice-card{min-width:0;padding:1.25rem;border:1px solid var(--line);border-radius:.8rem;background:var(--paper);overflow-wrap:anywhere}.choice-top{display:flex;flex-wrap:wrap;gap:.4rem}.choice-card h3{font-size:1.15rem;line-height:1.35;margin:.75rem 0 1rem}.choice-card h4,.audit-summary h4{font-size:.75rem;letter-spacing:.04em;text-transform:uppercase;margin:1rem 0 .35rem;color:#526057}.choice-card p{font-size:.9rem;margin:.3rem 0 0;line-height:1.6}.required-action{padding:1rem;background:var(--warning-bg);border-left:3px solid #bd7a31;border-radius:0 .5rem .5rem 0}.required-action h4:first-child{margin-top:0}.required-action p{font-weight:600}.required-action.correction{background:var(--danger-bg);border-color:var(--danger)}.choice-context{display:grid;grid-template-columns:1fr 1fr;gap:1.2rem}.choice-provenance{margin-top:1rem;padding-top:.75rem;border-top:1px solid var(--line)}.choice-card .meta{font-size:.75rem}.choice-evidence{margin-top:.6rem;font-size:.8rem}.choice-evidence summary{cursor:pointer;color:var(--blue);font-weight:700}.evidence-digest{display:block;font-size:.72rem}.choice-status{display:flex;flex-wrap:wrap;gap:.4rem;margin-top:1rem}.removal-note{padding:.6rem .8rem;background:#ecece5;border-radius:.4rem}.audit-summary{margin-top:.8rem}
@media(max-width:900px){.choice-grid{grid-template-columns:1fr}}
@media(max-width:620px){.choice-group{padding:1rem}.choice-card{padding:1rem}.choice-context{grid-template-columns:1fr;gap:0}.choice-card h3{font-size:1.05rem}.required-action{padding:.8rem}}
`

export const CLIENT = `
const root=document.querySelector('#app');let csrf='';let snapshot=null;
const el=(tag,attrs={},...children)=>{const node=document.createElement(tag);for(const [key,value] of Object.entries(attrs)){if(key==='class')node.className=value;else if(key==='text')node.textContent=String(value);else if(key==='disabled')node.disabled=Boolean(value);else if(key.startsWith('on'))node.addEventListener(key.slice(2).toLowerCase(),value);else node.setAttribute(key,String(value))}for(const child of children.flat()){if(child!==undefined&&child!==null)node.append(child instanceof Node?child:document.createTextNode(String(child)))}return node};
const short=value=>String(value).length>18?String(value).slice(0,9)+'…'+String(value).slice(-6):String(value);const date=value=>String(value).replace('T',' ').replace(/\\.000Z$|Z$/,' UTC');
const count=(value,singular)=>value+' '+singular+(value===1?'':'s');
const badge=(label,tone='')=>el('span',{class:'badge '+tone,text:label});const empty=message=>el('p',{class:'empty',text:message});
const safeLink=(url,label)=>{try{const parsed=new URL(url);if(!['http:','https:'].includes(parsed.protocol))return el('span',{class:'body-copy',text:url});return el('a',{class:'external',href:parsed.href,target:'_blank',rel:'noreferrer',text:label})}catch{return el('span',{class:'body-copy',text:url})}};
const limits=items=>items?.length?el('ul',{class:'limitations','aria-label':'Evidence limitations'},...items.map(item=>el('li',{},item.code+': '+item.detail))):null;
function status(message,isError=false){const prior=document.querySelector('.status');prior?.remove();const node=el('div',{class:'status'+(isError?' error':''),role:'status',text:message});document.body.append(node);setTimeout(()=>node.remove(),5000)}
function id(prefix='action'){const alphabet='0123456789ABCDEFGHJKMNPQRSTVWXYZ';let time=BigInt(Date.now()),head='';for(let i=0;i<10;i++){head=alphabet[Number(time&31n)]+head;time>>=5n}const bytes=crypto.getRandomValues(new Uint8Array(10));let random=0n;for(const byte of bytes)random=(random<<8n)|BigInt(byte);let tail='';for(let i=0;i<16;i++){tail=alphabet[Number(random&31n)]+tail;random>>=5n}return prefix+'_'+head+tail}
async function post(path,body,button){button.disabled=true;try{if(!csrf){const session=await fetch('/api/session');if(!session.ok)throw new Error('Could not establish action authority');csrf=(await session.json()).csrfToken}const response=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json','X-Factory-CSRF':csrf},body:JSON.stringify(body)});if(!response.ok){const result=await response.json().catch(()=>({}));throw new Error(result.error==='stale-action'?'The view changed. Refresh and try again.':'Action was not accepted.')}await load();status('Action recorded in append-only Factory history.')}catch(error){status(error.message||'Action failed.',true)}finally{button.disabled=false}}
function actionButton(label,kind,item,disputeActionId){const tone=kind==='confirm'?' good':kind==='supersede'?' danger':kind==='reject'?' danger secondary':' secondary';return el('button',{class:'button small'+tone,text:label,onClick:async event=>{const button=event.currentTarget;const body={actionId:id(),kind,expectedStateFingerprint:snapshot.decisions.stateFingerprint};if(kind==='supersede'){body.fromObservationId=item.pendingFromObservationId;body.toObservationId=item.observation.observationId;const note=prompt('Why should this observation replace the confirmed decision?');if(!note)return;body.note=note}else if(kind==='resolve'){body.disputeActionId=disputeActionId;const note=prompt('How was this dispute resolved?');if(!note)return;body.note=note}else{body.targetObservationId=item.observation.observationId;if(kind==='dispute'){const note=prompt('What is disputed?');if(!note)return;body.note=note}else if(kind==='reject'){const note=prompt('Optional rejection note');if(note)body.note=note}}await post('/api/actions/decision',body,button)}})}
function panel(id,title,subtitle,content){return el('section',{class:'panel',id},el('header',{class:'panel-head'},el('h2',{text:title}),el('p',{text:subtitle})),el('div',{class:'list'},...(content.length?content:[empty('Nothing recorded yet.')])))}
function renderSessions(){return snapshot.sessions.map(session=>el('article',{class:'item'},el('div',{class:'item-head'},el('div',{},el('h3',{text:session.provider+' session'}),el('p',{class:'meta mono',text:session.sessionKey})),el('div',{class:'badges'},badge('Status · '+(session.active?'active':'ended'),session.active?'good':''),badge(count(session.turns.length,'turn')))),el('div',{class:'sublist'},...session.turns.map((turn,index)=>el('div',{class:'subitem'},el('strong',{text:'Turn '+(index+1)+' · '+(turn.branch||'detached HEAD')}),el('p',{class:'meta',text:date(turn.capturedAt)+' · watermark '+turn.evidenceWatermark+' · '+count(turn.eventCount,'event')+' · '+count(turn.transcriptCount,'transcript entry')}),limits(turn.limitations))))))}
function renderObservations(){return snapshot.repositoryObservations.map(item=>el('article',{class:'item'},el('div',{class:'item-head'},el('div',{},el('h3',{text:item.branch||'Detached HEAD'}),el('p',{class:'meta mono',text:short(item.observationId)+' · '+date(item.observedAt)})),el('div',{class:'badges'},badge(item.exact?'Exact snapshot':'Raced snapshot',item.exact?'good':'danger'),item.detached?badge('Detached','warn'):badge(item.changedPaths+' changed'))),item.head?el('p',{class:'body-copy mono',text:'HEAD '+item.head}):null,limits(item.limitations)))}
function renderPrs(){return snapshot.pullRequests.map(pr=>{const links=snapshot.associations.filter(item=>item.pullRequestObservationId===pr.observationId);return el('article',{class:'item'},el('div',{class:'item-head'},el('div',{},el('h3',{text:'Pull request #'+pr.number}),el('p',{class:'meta mono',text:short(pr.repositoryKey)+' · '+date(pr.observedAt)})),el('div',{class:'badges'},badge('Data · '+pr.availability,pr.availability==='available'?'good':'danger'),badge('Commits · '+pr.completeness,pr.completeness==='complete'?'good':'warn'),badge('State · '+pr.state))),pr.url?safeLink(pr.url,'Open pull request ↗'):null,links.length?el('div',{class:'sublist'},...links.map(link=>el('div',{class:'subitem '+(link.authority==='exact'?'':'high')},el('strong',{text:link.authority==='exact'?'Exact association':link.authority==='asserted'?'Human-asserted association':'Verified invalidation'}),el('p',{class:'meta mono',text:link.sessionKey+' · '+link.kind+' · '+short(link.evidenceId)})))):el('p',{class:'body-copy',text:'No exact Session association. Same-branch or temporal signals remain ambiguous context.'}),limits(pr.limitations))})}
function evidence(items){return items?.length?el('details',{class:'choice-evidence'},el('summary',{text:count(items.length,'evidence citation')}),...items.map(item=>el('p',{class:'meta'},el('strong',{text:item.role}),item.locator?' · '+item.locator:null,el('code',{class:'mono evidence-digest',text:'SHA-256 '+item.digest})))):null}
function renderReviews(){return snapshot.reviews.slice().reverse().map(review=>el('article',{class:'item'},el('div',{class:'item-head'},el('div',{},el('h3',{text:(review.disposition==='partial'?'Partial ':review.disposition==='failed'?'Failed ':'Completed ')+review.subject.kind+' audit'}),el('p',{class:'meta mono',text:short(review.reviewId)+' · '+date(review.completedAt)})),el('div',{class:'badges'},badge('Result · '+review.disposition,review.disposition==='complete'?'good':review.disposition==='partial'?'warn':'danger'),review.coverageAccepted?badge('Coverage · accepted','info'):badge('Coverage · '+review.coverageEffect))),review.failureReason?el('p',{class:'body-copy',text:'Failure: '+review.failureReason}):null,el('p',{class:'body-copy',text:count(review.choiceCount,'audited choice')}),review.summary?el('div',{class:'audit-summary'},el('h4',{text:'Scope reviewed'}),el('p',{class:'body-copy',text:review.summary.reviewed}),review.summary.noChoiceRationale?el('div',{},el('h4',{text:'Why no undeclared choices were found'}),el('p',{class:'body-copy',text:review.summary.noChoiceRationale})):null,review.summary.trivialDiscretionCount!==undefined?el('p',{class:'meta',text:count(review.summary.trivialDiscretionCount,'minor naming or cosmetic choice')+' summarized.'}):null,evidence(review.summary.evidence)):el('p',{class:'meta',text:'No audit summary was submitted.'}),limits(review.limitations),review.disposition==='partial'&&!review.coverageAccepted?el('div',{},el('p',{class:'action-note',text:'Recording partial coverage keeps every missing-evidence warning visible.'}),el('div',{class:'actions'},el('button',{class:'button small secondary',text:'Record reviewed-partial coverage',onClick:event=>post('/api/actions/coverage',{reviewId:review.reviewId},event.currentTarget)}))):null))}
function renderTriggers(){return snapshot.triggers.map(trigger=>el('article',{class:'item'},el('div',{class:'item-head'},el('div',{},el('h3',{text:'Watermark '+trigger.evidenceWatermark}),el('p',{class:'meta mono',text:trigger.sessionKey+' · '+short(trigger.triggerId)})),el('div',{class:'badges'},badge('Coverage · '+trigger.coverage,trigger.coverage==='reviewed'?'good':trigger.coverage==='pending'?'danger':'warn'),badge('Capture · '+trigger.materialization,trigger.materialization==='complete'?'good':'warn'))),limits(trigger.limitations)))}
const choiceLabels={'needs-user':['Needs your decision','Choose the direction; reversible provisional calls are shown first.','warn'],'unsound':['Requires correction','Redo the decision from the stated property, not a patch.','danger'],'sound':['Sound choices','Acknowledge the decisions future work will inherit.','good']};
function choiceCard(item){const choice=item.observation;const terminal=item.scope==='unclassified'||['invalid','rejected','superseded','removed'].includes(item.lifecycle);const controls=[];const dispute=snapshot.unresolvedDisputes.find(action=>action.targetObservationId===choice.observationId);if(!terminal&&item.humanStatus!=='confirmed'&&item.humanStatus!=='disputed')controls.push(actionButton('Confirm recorded choice','confirm',item));if(['proposal','pending-supersession'].includes(item.lifecycle))controls.push(actionButton('Reject','reject',item));if(!terminal&&item.humanStatus!=='disputed')controls.push(actionButton('Dispute','dispute',item));if(dispute)controls.push(actionButton('Resolve dispute','resolve',item,dispute.actionId));if(item.lifecycle==='pending-supersession')controls.push(actionButton('Replace confirmed decision','supersede',item));
return el('article',{class:'choice-card','data-choice':choice.choiceKey},el('div',{class:'choice-top'},el('span',{class:'badge choice-verdict '+choiceLabels[choice.verdict][2],text:choiceLabels[choice.verdict][0]}),badge(choice.confidence+' confidence',choice.confidence==='low'?'warn':''),badge('Scope · '+item.scope,item.scope==='canonical'?'good':'info'),item.scope==='canonical'&&item.priority==='high'?badge('Canonical attention','warn'):null),el('h3',{text:choice.headline}),choice.effect==='remove'?el('p',{class:'removal-note',text:'Explicit removal · this choice is no longer present.'}):choice.effect==='contradict'?el('p',{class:'removal-note',text:'Explicit contradiction · this evidence establishes incompatible meaning.'}):null,choice.verdict==='needs-user'?el('div',{class:'required-action'},el('h4',{text:'Provisional call'}),el('p',{text:choice.provisionalCall}),el('h4',{text:'How to reverse it'}),el('p',{text:choice.reversal})):choice.verdict==='unsound'?el('div',{class:'required-action correction'},el('h4',{text:'Corrected decision'}),el('p',{text:choice.correctedDecision})):null,el('div',{class:'choice-story'},el('h4',{text:'What happens'}),el('p',{text:choice.scenario}),el('div',{class:'choice-context'},el('div',{},el('h4',{text:'The gap'}),el('p',{text:choice.gap})),el('div',{},el('h4',{text:'Future reach'}),el('p',{text:choice.reach}))),el('h4',{text:'Why this verdict'}),el('p',{text:choice.rationale})),el('div',{class:'choice-provenance'},el('p',{class:'meta',text:'Implementation point · '+choice.when}),el('p',{class:'meta',text:'Observed '+date(choice.observedAt)}),el('p',{class:'meta mono',text:'Source review '+choice.reviewId+' · entry '+choice.reviewEntryId}),evidence(choice.evidence)),item.scope==='unclassified'?badge('Read-only · scope unavailable'):el('div',{class:'choice-status'},badge('State · '+item.lifecycle,item.priority==='high'?'warn':''),badge('Human · '+item.humanStatus,item.humanStatus==='confirmed'?'good':item.humanStatus==='disputed'?'danger':'')),item.lifecycle==='pending-supersession'?el('p',{class:'action-note',text:'Confirm records human support. Replace changes the current selection immediately; both remain in append-only history.'}):null,controls.length?el('div',{class:'actions'},...controls):null)}
function renderDecisions(){if(!snapshot.decisions)return [el('div',{class:'item'},el('p',{class:'body-copy',text:'Configure a canonical branch to classify choice evidence.'}))];const groups=snapshot.decisions.groups.filter(group=>group.choices.length);const notice=snapshot.decisions.stateFingerprint===null?[el('p',{class:'item',text:'Canonical branch is not configured. These choices are read-only; scope and human status are unavailable.'})]:[];return [...notice,...(groups.length?groups.map(group=>el('section',{class:'choice-group','data-verdict':group.verdict},el('header',{class:'choice-group-head'},el('h3',{text:choiceLabels[group.verdict][0]}),el('p',{text:choiceLabels[group.verdict][1]+' Least-confident first.'})),el('div',{class:'choice-grid'},...group.choices.map(choiceCard)))):[empty('No audited choices yet. Review history explains completed empty audits.')])];}
function renderDiagnostics(){return snapshot.diagnostics.map(item=>el('article',{class:'item'},el('div',{class:'item-head'},el('strong',{text:item.message}),badge(item.priority,item.priority==='high'?'danger':''))))}
function renderReady(){root.className='shell';root.replaceChildren(el('header',{class:'masthead'},el('div',{},el('div',{class:'brand'},el('span',{class:'mark',text:'F'}),el('div',{},el('p',{class:'eyebrow',text:'Local evidence'}),el('h1',{text:'Factory'}))),el('p',{class:'subtitle',text:'Inspect what coding agents changed, what evidence was reviewed, and which decisions still need a human.'})),el('div',{class:'head-actions'},el('span',{class:'branch',text:snapshot.canonicalBranch?'Canonical · '+snapshot.canonicalBranch:'Canonical branch unset'}),el('button',{class:'button secondary',text:'Refresh',onClick:()=>load()}))),el('nav',{class:'nav','aria-label':'Jump to evidence section'},...['sessions','pull-requests','reviews','decisions','diagnostics'].map(id=>el('a',{href:'#'+id,text:id.replace('-',' ')+' ↓'}))),el('main',{id:'content'},el('section',{class:'summary','aria-label':'Repository evidence summary'},...[[snapshot.counts.sessions,'Sessions'],[snapshot.counts.turns,'Turns'],[snapshot.counts.pullRequests,'Pull requests'],[snapshot.counts.reviews,'Reviews'],[snapshot.counts.pendingTriggers,'Pending triggers',snapshot.counts.pendingTriggers?'attention':''],[snapshot.counts.highPriorityDecisions,'Decision alerts',snapshot.counts.highPriorityDecisions?'attention':'']].map(([value,label,tone])=>el('div',{class:'metric '+(tone||'')},el('strong',{text:value}),el('span',{text:label})))),panel('decisions','Choice ledger','Analyzer judgment and human confirmation remain separate',renderDecisions()),el('div',{class:'layout'},el('div',{class:'column'},panel('sessions','Sessions & turns','Provider identity stays visible',renderSessions()),panel('observations','Branch observations','Git context, never a work identity',renderObservations()),panel('pull-requests','Pull requests','Exact proof stays distinct from weak context',renderPrs()),panel('reviews','Review history','Audit scope, completion, and evidence limitations',renderReviews())),el('aside',{class:'column','aria-label':'Attention and decisions'},panel('triggers','Coverage','Stop-by-Stop review status',renderTriggers()),panel('diagnostics','Diagnostics','High-priority integrity and stale-action facts',renderDiagnostics())))))}
function renderUnavailable(){root.className='';root.replaceChildren(el('main',{class:'error-page',id:'content'},el('p',{class:'eyebrow',text:'Read-only safety state'}),el('h1',{text:snapshot.title}),el('p',{class:'body-copy',text:snapshot.message}),el('button',{class:'button secondary',text:'Try again',onClick:()=>load()})))}
async function load(){root.setAttribute('aria-busy','true');try{const response=await fetch('/api/snapshot',{headers:{Accept:'application/json'}});if(!response.ok)throw new Error('Snapshot unavailable');snapshot=await response.json();snapshot.state==='ready'?renderReady():renderUnavailable();root.dataset.ready='true'}catch{root.className='';root.replaceChildren(el('main',{class:'error-page',id:'content'},el('p',{class:'eyebrow',text:'Connection lost'}),el('h1',{text:'Factory evidence is unavailable'}),el('p',{class:'body-copy',text:'The short-lived local server could not rebuild this view.'})));root.dataset.ready='error'}finally{root.removeAttribute('aria-busy')}}
load();
`

{
  "name": "@factory/repository",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./internal/admission": "./src/admission-internal.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check-types": "tsc --noEmit",
    "lint": "oxlint src test",
    "test": "bun run test/docker.ts && bun run test/git-docker.ts"
  },
  "dependencies": {
    "@factory/contract": "workspace:*",
    "@factory/sanitization": "workspace:*"
  }
}

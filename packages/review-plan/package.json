{
  "name": "@factory/review-plan",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/public.ts",
    "./testing": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check-types": "tsc --noEmit",
    "lint": "oxlint src test",
    "test": "bun test test/planning.test.ts && bun run test/docker.ts"
  },
  "dependencies": {
    "@factory/capture": "workspace:*",
    "@factory/contract": "workspace:*",
    "@factory/domain": "workspace:*",
    "@factory/github": "workspace:*",
    "@factory/repository": "workspace:*",
    "@factory/sanitization": "workspace:*"
  }
}

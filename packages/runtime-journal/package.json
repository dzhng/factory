{
  "name": "@factory/runtime-journal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && bun build src/index.ts --target=node --outfile=dist/index.js --external bun:sqlite",
    "check-types": "tsc --noEmit",
    "lint": "oxlint src test",
    "test": "bun run test/docker.ts"
  },
  "dependencies": {
    "@factory/contract": "workspace:*",
    "@factory/repository": "workspace:*"
  },
  "engines": {
    "node": ">=22.13"
  }
}

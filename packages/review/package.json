{
  "name": "@factory/review",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check-types": "tsc --noEmit",
    "lint": "oxlint src test",
    "test": "bun test"
  },
  "dependencies": {
    "@factory/contract": "workspace:*",
    "@factory/domain": "workspace:*",
    "@factory/repository": "workspace:*",
    "@factory/reviewer": "workspace:*"
  }
}

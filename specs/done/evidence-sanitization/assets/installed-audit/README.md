# Installed sanitization and choice-audit evidence

[The synthetic report](report.json) passed on clean production revision
`33ea517a94afc54f627c0fa1ec439e4a3d76b366`. The native Linux executable came
from the existing release archive builder, not a development CLI. Its consumer
PATH contained no Bun. The report records the commands, exact artifact identity,
accepted three-verdict ledger, and fifteen completed checks.

## Authority

Both provider transcripts and hooks cross the real capture boundary. The journey
retains long reasoning, reduces result context, discovers nested ignored env
values, and preserves original provider files. It blocks one selected CAS write,
then retries the frozen Stop after removing the env and provider transcript.
Earlier safe object prefixes are allowed. Separate crash labs own SIGKILL and
all-prefix recovery; host-shared Docker mounts do not certify concurrent locking.

Synthetic provider executables call the actual isolated submission server.
Workspace and PR audits publish through the real repository owner; PR authority
is patch and metadata, not the optional exact-SHA source-capture seam. Localhost
actions include a secret note and an identical retry after env rotation. A fresh
clone containing only committed `.factory` reconstructs available source and
citations and retains the same three-verdict interface projection and attention.
All 92 physical portable files, including unreferenced objects and decoded JSON
keys/paths, pass the seeded-secret scan. Negative controls prove the scanner
detects escaped and encoded secrets outside referenced history.

The default-image check removes the override and records the native CLI's exact
Docker pull of the published `0ecb58cc…` digest, refusing execution at that external
boundary. The old `b08ebef` archive failed this assertion with `8b4d1bb6…`; the
final archive passed. This establishes default selection, not the contents of the
remote image. Its separate packaged-image probe owns that authority. No real
provider credentials or model judgment are claimed.

The separate published-image gate supplied by the integration owner inspected
GH Actions run `34049008871` (source `3e8288f`) and OCI digest
`sha256:0ecb58ccbec9b1bafa64f403ebc56963a41dec3f905edf37355d6fca9bbc25ee`.
Its packaged audit server SHA-256 was
`88702047932c786b6e62c26386cbd24ecd2a6982789e286276084977ddd115f9`.
With networking disabled, actual Claude `2.1.261` advertised the three tools,
Codex `0.144.4` registered them for deferred discovery, and the packaged server
accepted a three-verdict draft of 3,582 bytes. These are image-content and client
registration probes, not real inference. The harness's `provider-tools-probe.ts`
and `audit-submission-probe.ts` own those reproducible checks.

## Reproduction

Build the clean revision with `scripts/build-release.ts`, version `0.1.0-audit`
and target `bun-linux-x64-baseline`, then pass its archive and adjacent manifest
to the existing feature runner. This observed invocation used temporary artifacts:

```sh
bun run lab:installed-audit \
  --archive /tmp/factory-installed-audit-final-33ea517/factory-v0.1.0-audit-linux-x64-baseline.tar.gz \
  --manifest /tmp/factory-installed-audit-final-33ea517/factory-v0.1.0-audit-linux-x64-baseline.json \
  --manifest-sha256 5f7023c87d43d829c97e18ffbf68eb92e072a829a091a47e93156d23032d6919
```

The same clean revision also built the `bun-darwin-arm64` archive. Its manifest
digest was `d6ea65cd14c4b26cf23b0203c86f88b17100babd774c9e76e82056f796011b8b`,
and executable digest was
`33f836db16ac745e95dc023a930dc15aedcef2689c2f2bfab241c66a9481553f`.
An environment-cleared native `--version` returned `0.1.0-audit`; this is build
and launch authority, not a host capture journey. Platform CI and repository-wide
closeout gates are recorded separately. These artifacts are local certification
candidates, not a tagged or published release.

## Complementary closeout gates

[CI run 34050364687](https://github.com/dzhng/factory/actions/runs/34050364687)
passed at `8b7407a`: repository quality checks, native Linux release certification,
native macOS packaging, and both artifact-attestation jobs. The macOS CI job
explicitly has package-only authority; it does not run a Docker review journey.

Local closeout also passed the repository build, format, lint, type, and test
gates on the final production revision, alongside the existing review CLI/PR
workbenches, isolation journey, and responsive browser gates. The isolated
release fixture uses private synthetic credentials so its container identity
matches the consumer that reads private submissions. Its nine-step installed
journey passed after reproducing the public-fixture ownership failure with the
same native archive. These checks complement, rather than replace, the feature
journey's physical-byte and recovery assertions.

Whole-feature code review and two fresh archived-claim audits completed. The
claim audits corrected the distinction between pre-merge match candidates and
merged spans, and between an unfinished empty summary and a completed empty
audit. Those corrections describe the verified behavior; they do not relax
publication or completion validation.

# Release evidence visual review

This report was generated by the exact macOS arm64 artifact at revision
`0f2ba19ba7b5b8b957e5739e17f4ac8969af9db1`. Its deterministic install,
capture, review, UI action, diagnostics, upgrade, and uninstall journeys passed,
as did one real Codex review and one real Claude review through the pinned
production image. Both existing CLI logins were discovered automatically. The
report deliberately says **NOT RELEASE-CERTIFIED** because GitHub Release
publication and its resulting artifact attestation remain unavailable.

The exact 1280-pixel-wide full-page capture and 2× crops of the summary,
candidate identity, authority table, and journey table received fresh, unprimed
visual critiques. The first passes found wrapped capability names, a detached
digest status mark, and weak blocker hierarchy; the report generator was fixed
and recaptured. The final critique accepted the report with no clipping,
overflow, alignment error, rendering artifact, or missing visible section. The
accepted capture is `screenshot.png`; temporary crops were used only for zoomed
inspection and are not standalone journey evidence.
